import { useState } from 'react';
export default function ReservationForm() {
  const [form, setForm] = useState({ name: '', people: '' }); const [error, setError] = useState(''); const [confirmation, setConfirmation] = useState('');
  const submit = (event) => { event.preventDefault(); const name = form.name.trim(); const people = Number(form.people); if (!name || form.people === '') { setError('Completa todos los campos obligatorios.'); setConfirmation(''); return; } if (!Number.isInteger(people) || people < 1) { setError('El número de personas debe ser un entero mayor que cero.'); setConfirmation(''); return; } setError(''); setConfirmation(`¡Gracias, ${name}! Tu solicitud de reservación para ${people} ${people === 1 ? 'persona ha' : 'personas ha'} sido registrada.`); setForm({ name: '', people: '' }); };
  return <section className="card" id="reservacion"><p className="section-label">Da el primer paso</p><h2>Reservación y contacto</h2><form className="reservation-form" onSubmit={submit} noValidate><label>Nombre completo *<input type="text" value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder="Escribe tu nombre" /></label><label>Número de personas *<input type="number" min="1" step="1" value={form.people} onChange={(event) => setForm({ ...form, people: event.target.value })} placeholder="Ej. 2" /></label><button className="button" type="submit">Enviar solicitud</button></form>{error && <p className="form-message form-message--error" role="alert">{error}</p>}{confirmation && <p className="form-message form-message--success" role="status">{confirmation}</p>}</section>;
}
